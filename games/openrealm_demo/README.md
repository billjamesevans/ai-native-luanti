# OpenRealm Demo

This is the public-safe Luminara creator playground. It must not contain private
family-server worlds, proprietary assets, copied Minecraft assets, or local
credentials.

The profile exists to make the OpenRealm creator loop visible without bundling
private family content. Keep engine/runtime improvements in the fork, and keep
world-specific content in standalone mods.
