# v0.3 AI-Native Proving Ground Roadmap

Status: next milestone after v0.2 product-loop closure.

## Direction

v0.2 closed the first public-safe product loop: clean `ai_runtime` profile
verification, operator status/task control, low-power evidence, compatibility
inventory discovery, staged apply smoke, and stricter Minecraft-parity target
bands.

v0.3 should turn that into a stronger proving ground. The goal is not a content
pack and not a private family-server fork. The goal is a repeatable side-by-side
AI-native server lane where agents can do more useful work under explicit
budgets, rollback, audit, operator controls, and benchmark gates.

Compatibility/import remains behind runtime gates. It can expand only when the
runtime proves the apply path is bounded, reversible, observable, and
public-safe.

## Current Baseline

- Latest verified Pi fork checkout: `dd6082dd8`.
- Pi fork test service: `ai-native-luanti-test.service` on UDP `30001`.
- Family server: `luanti-family.service` on UDP `30000`.
- Latest Pi deploy: backup-first side-by-side deploy on June 29, 2026.
- Backup artifact: `raspberrypi_luanti_20260629-073722.tgz`.
- Backup SHA256:
  `507fb8c44f9761a0ea8c670802b74fd20b98e7cc61e3c1d90f891a5bdfb015b4`.
- Pi fork verification: `TestAIRuntime` passed on the Pi, fork service active,
  family service active, UDP `30000` and `30001` listening.
- Quick low-power evidence: `pass` for clean `ai_runtime`, headless client
  load, join-log latency proxy, server-step workload, compatibility import
  staging pilot, zero fork restarts, zero actionable warnings, zero server log
  errors, and no ranked follow-up issue seeds. Next target: one-hour soak.

## Milestone Issues

- #237 Pi proving-ground soak: promoted fork service evidence beyond smoke.
- #238 Multi-player and agent scale gate for `ai_runtime`.
- #239 Bounded pathfinding and perception slice for first-party agents.
- #240 Operator alpha release runbook for the side-by-side fork service.
- #241 Family-plugin boundary extraction from `family_creatures` prototypes.
- #242 Compatibility apply expansion behind runtime gates.

## Execution Order

1. Prove the side-by-side Pi service can run longer than a deploy smoke.
2. Raise synthetic player and agent scale gates on the clean profile.
3. Add bounded navigation/perception where agents currently have direct-line
   helper movement.
4. Package the operator promote/verify/rollback loop so alpha candidates are
   repeatable.
5. Extract reusable family-plugin boundaries without copying private family
   content into the fork.
6. Expand compatibility apply only after the runtime evidence above remains
   clean.

## Public-Safe Boundary

The milestone must not commit or retain:

- private family worlds or player-private data;
- `spacebase`, `themepark`, `disneyland100`, or showcase builds;
- copied Minecraft assets, server jars, marketplace content, or closed gameplay
  data;
- provider prompts, API keys, host credentials, or private payloads;
- live-family-world mutation from compatibility/import tooling.

The family server remains the proving ground for ideas, not the source tree for
the main fork. Generic runtime primitives can move into the fork only when they
have public-safe examples, focused tests, benchmark coverage, and clear operator
controls.
