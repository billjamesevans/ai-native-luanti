# v0.3 AI-Native Proving Ground Roadmap

Status: next milestone after v0.2 product-loop closure.

## Direction

v0.2 closed the first public-safe product loop: clean `ai_runtime` profile
verification, operator status/task control, low-power evidence, compatibility
inventory discovery, staged apply smoke, and stricter Minecraft-parity target
bands.

v0.3 should turn that into a stronger proving ground. The goal is not a content
pack and not a private family-server fork. The goal is a repeatable side-by-side
AI-native server lane where agents can do more useful work under explicit
budgets, rollback, audit, operator controls, and benchmark gates.

Compatibility/import remains behind runtime gates. It can expand only when the
runtime proves the apply path is bounded, reversible, observable, and
public-safe.

## Current Baseline

- Latest side-by-side Pi fork checkout: `062c6b2ef`.
- Pi fork test service: `ai-native-luanti-test.service` on UDP `30001`.
- Family server: `luanti-family.service` on UDP `30000`.
- Latest Pi deploy: backup-first side-by-side deploy on June 30, 2026, after
  hardening streamed Agents SDK build-planning through the fork sidecar.
- Backup artifact: `raspberrypi_luanti_20260630-154507.tgz`.
- Backup SHA256:
  `43910e79b603530ac341eb3f95cdf14c266cb29e9b98ce1efb924f2a732a63f6`.
- Pi fork verification: fork service active, family service active, UDP `30000`
  and `30001` listening, and the Agents SDK adapter active on loopback TCP
  `8766`.
- Live Agents SDK sidecar evidence for `062c6b2ef`: provider-backed build
  probes for `build a fire`, `build me a fire and only a fire`, and
  `build a wall of tnt` returned `agents_sdk_function_tool`,
  `required_tool_calls_satisfied = true`, no repair pass, no rejected tool
  calls, and ready `luanti_build_action_plan_v1` decisions for `fire`, `fire`,
  and `tnt_wall`.
- Current agent quality gate on the Pi for `062c6b2ef`: `pass`; 5/5 live prompt
  eval cases passed, 4/4 required agentic tool cases passed, model adapter
  failures/timeouts were zero, active adapter-contract failures were zero,
  compatibility import staging pilot was `pass`, and review/action items were
  zero.
- Request/response log gate from the live Pi Agents SDK adapter for
  `062c6b2ef`: `pass` over 383 retained public-safe entries; `build a fire`,
  `build me a fire and only a fire`, `build a wall of tnt`, and
  `build a small shelter` all had retained tool evidence, 4/4 cases passed,
  no private-content violations, and no failures. The strict fire prompt
  selected `fire`, the TNT prompt selected `tnt_wall` with no danger refusal,
  and the generated shelter selected `generated_shelter_floor` with
  `propose_build_option`, `select_build_option`, and `plan_build_actions`
  traces.
- Request/response-gate-fed memory refresh from the retained Pi adapter log:
  `ready`; one gate file read, four gate cases read, four candidates added, four
  replayable prompt-memory cases emitted (`build_fire`, `fire_only_strict`,
  `tnt_wall`, and `generated_shelter_floor`), review queue `ready`, combined
  agent quality gate `pass`, zero violations, and zero attention items. The
  cases remain review-required before default gating, but they are now retained
  as replayable prompt-eval memory instead of disappearing in raw-log history.
- Promoted one-hour low-power evidence for `062c6b2ef`: `pass` for 13/13
  samples over 4178.69 seconds after the backup-first side-by-side deploy.
  Clean `ai_runtime`, 2 attempted/connected/completed headless synthetic
  players, `ai_runtime_scale_gate=pass`, compatibility import staging pilot
  `pass`, zero fork restarts, zero actionable warnings, zero server log
  errors, public-safe output, backup SHA recorded, and no ranked follow-up
  issue seeds. Resource maxima were average process CPU 83.895%, max interval
  CPU 113.643%, and max RSS 76.953 MB.
- Minecraft-parity report after refreshing accepted local and low-power lanes:
  all engine/runtime, game-content, first-party plugin, and operator-experience
  gap areas `pass`; actionable scorecard empty; report privacy scan `passed`.
  Current public-safe report still uses accepted baseline lanes; the next
  promoted Pi target is overnight evidence for `062c6b2ef`.

## Milestone Issues

- #237 Pi proving-ground soak: promoted fork service evidence beyond smoke
  (completed).
- #238 Multi-player and agent scale gate for `ai_runtime` (completed).
- #239 Bounded pathfinding and perception slice for first-party agents
  (merged in PR #248 and deployed side by side on the Pi).
- #240 Operator alpha release runbook for the side-by-side fork service
  (merged in PR #249).
- #241 Family-plugin boundary extraction from `family_creatures` prototypes
  (merged in PR #250).
- #242 Compatibility apply expansion behind runtime gates (merged in PR #251):
  larger public-safe staging rehearsal covers 18 node writes, five apply
  chunks, 10 touched mapblocks, five rollback records, five rollback execution
  chunks, metadata-only world-import policy, resource-pack no-mutation policy,
  and refusal gates for missing approval, missing rollback policy,
  unsafe/private payloads, non-staging targets, and over-budget writes.
- #252 Clean alpha package and dev-fixture separation (closed).
- #253 Promoted Pi one-hour and overnight evidence for current alpha.
- #254 First-party AI agent productization lane. Current slice: streamed Agents
  SDK tool-plan execution is deployed and green on the Pi, with prompt-memory
  and adapter-contract gates feeding the improvement loop.
- #255 Compatibility import scale-up after staged apply pilot.
- #256 Minecraft parity benchmark expansion.
- #257 Contributor release automation and project operating loop
  (completed by PR #269 and PR #277; now enforced by the alpha release gate).

## Execution Order

1. Prove the side-by-side Pi service can run longer than a deploy smoke
   (one-hour proof is clean for `062c6b2ef`; next promoted target is
   overnight).
2. Raise synthetic player and agent scale gates on the clean profile
   (completed).
3. Add bounded navigation/perception where agents currently have direct-line
   helper movement (merged and deployed side by side).
4. Package the operator promote/verify/rollback loop so alpha candidates are
   repeatable (merged).
5. Extract reusable family-plugin boundaries without copying private family
   content into the fork (merged).
6. Expand compatibility apply only after the runtime evidence above remains
   clean (merged).
7. Lock down the clean alpha package so dev fixtures stay outside the
   player-facing profile (closed).
8. Promote overnight Pi evidence for the current alpha candidate.
9. Continue productizing first-party agents, then scale compatibility/import and
   parity benchmarks through the same gates.

## Public-Safe Boundary

The milestone must not commit or retain:

- private family worlds or player-private data;
- `spacebase`, `themepark`, `disneyland100`, or showcase builds;
- copied Minecraft assets, server jars, marketplace content, or closed gameplay
  data;
- provider prompts, API keys, host credentials, or private payloads;
- live-family-world mutation from compatibility/import tooling.

The family server remains the proving ground for ideas, not the source tree for
the main fork. Generic runtime primitives can move into the fork only when they
have public-safe examples, focused tests, benchmark coverage, and clear operator
controls.
